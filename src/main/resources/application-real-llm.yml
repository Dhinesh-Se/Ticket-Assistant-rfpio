llm:
  real:
    endpoint: ${LLM_ENDPOINT}
    api-key: ${LLM_API_KEY}
    model: ${LLM_MODEL:gpt-4.1-mini}
    auth-header: ${LLM_AUTH_HEADER:api-key}
    auth-prefix: ${LLM_AUTH_PREFIX:}
  timeout-seconds: ${LLM_TIMEOUT_SECONDS:10}
